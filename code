function main() -> int{
  return fib(20);
}

function fib(num: int) -> int{
  a: int = 1;
  b: int = 1;
  for (i: int = 2;i < num;i += 1) {
    tmp: int = a+b;
    a = b;
    b = tmp;
  }
  return b;
}
